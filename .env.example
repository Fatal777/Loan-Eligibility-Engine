# =====================================================
# LOAN ELIGIBILITY ENGINE - ENVIRONMENT CONFIGURATION
# =====================================================
# Copy this file to .env and fill in your values

# =====================================================
# AWS Configuration
# =====================================================
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1

# =====================================================
# RDS PostgreSQL Database (Loan Eligibility Data)
# =====================================================
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=loan_eligibility
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Alias for Lambda (same as above)
LOAN_DB_HOST=${DB_HOST}
LOAN_DB_PORT=${DB_PORT}
LOAN_DB_NAME=${DB_NAME}
LOAN_DB_USER=${DB_USER}
LOAN_DB_PASSWORD=${DB_PASSWORD}

# =====================================================
# n8n Configuration
# =====================================================
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678

# n8n Authentication
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=your_secure_password

# n8n Encryption Key (generate a secure random string)
N8N_ENCRYPTION_KEY=your_32_character_encryption_key

# =====================================================
# n8n Internal Database (PostgreSQL in Docker)
# =====================================================
N8N_DB_HOST=postgres
N8N_DB_PORT=5432
N8N_DB_NAME=n8n
N8N_DB_USER=n8n
N8N_DB_PASSWORD=n8n_secure_password

# =====================================================
# AI API Keys (choose one or both)
# =====================================================
GEMINI_API_KEY=your_gemini_api_key
OPENAI_API_KEY=your_openai_api_key

# =====================================================
# n8n Webhook URL (for Lambda to trigger workflows)
# =====================================================
N8N_WEBHOOK_URL=http://your-n8n-server:5678/webhook

# =====================================================
# Optional: pgAdmin Configuration
# =====================================================
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin_password

# =====================================================
# Timezone
# =====================================================
TIMEZONE=UTC
